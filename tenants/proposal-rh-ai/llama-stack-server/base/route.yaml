apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: llamastack-server
spec:
  to:
    kind: Service
    name: llamastack-server
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
